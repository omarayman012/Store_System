@model List<Store_Sys.Models.InFiles>


<style>
    .PrimaryBtn {
        background-color: rgb(15, 103, 177) !important;
        color: white; /* لون النص */
        border: none; /* إخفاء الإطار */
        padding: 10px 20px; /* ضبط الحشوة */
        text-align: center; /* محاذاة النص في الوسط */
        text-decoration: none; /* إزالة التسطير عن النص */
        display: inline-block; /* عرض الزر كعنصر سطري */
        font-size: 16px; /* حجم النص */
        margin: 4px 2px; /* الهوامش */
        cursor: pointer; /* تغيير شكل المؤشر عند التحويم */
        border-radius: 4px; /* تقويس الزوايا */
    }

    .PrimaryBtn:hover {
        color: white !important;
    }
    /* From Uiverse.io by alexmaracinaru */
    .cta {
        position: relative;
        margin: auto;
        padding: 12px 18px;
        transition: all 0.2s ease;
        border: none;
        background: none;
        cursor: pointer;
    }

        .cta:before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            display: block;
            border-radius: 50px;
            background: #b1dae7;
            width: 45px;
            height: 45px;
            transition: all 0.3s ease;
        }

        .cta span {
            position: relative;
            font-family: "Ubuntu", sans-serif;
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 0.05em;
            color: #234567;
        }

        .cta svg {
            position: relative;
            top: 0;
            margin-left: 10px;
            fill: none;
            stroke-linecap: round;
            stroke-linejoin: round;
            stroke: #234567;
            stroke-width: 2;
            transform: translateX(-5px);
            transition: all 0.3s ease;
        }

        .cta:hover:before {
            width: 100%;
            background: #b1dae7;
        }

        .cta:hover svg {
            transform: translateX(0);
        }

        .cta:active {
            transform: scale(0.95);
        }

</style>



<div class="mt-5">
    <div id="personal-info" class="shadow">
        <span class="bg-color"> بيانات مدخلات المستندات </span>
        <p>
            هذه الصفحة تعرض بيانات كل الجلسات الخاصة بمدخلات المستندات
        </p>
    </div>

    <div id="personal-info" class="shadow">
        <div class="row my-3">
            <div class="col-sm-12 col-md-6 col-lg-2">
                <a href="@Url.Action("AddFile", "InFiles")">
                    <button class="cta">
                        <span>إضافة مستند</span>
                        <svg width="15px" height="15px" viewBox="0 0 20 20">
                            <line x1="10" y1="3" x2="10" y2="17" stroke="black" stroke-width="2"></line>
                            <line x1="3" y1="10" x2="17" y2="10" stroke="black" stroke-width="2"></line>
                        </svg>
                    </button>
                </a>
            </div>
        </div>
        @{
            var counter = 1;
        }

        <div class="row">
            <form>
                <div class="row my-3">
                    <div class="col-sm-12 col-md-4 col-lg-3 col-xl-2 mt-2">
                        <label for="searchName" class="mb-2">بحث برقم المستند</label>
                        <input type="text" id="searchName" name="searchName" class="form-control" placeholder=" بحث برقم المستند">
                    </div>

                    <div class="col-sm-12 col-md-4 col-lg-3 col-xl-2 mt-2">
                        <label for="searchName" class="mb-2">بحث برقم الطلب</label>
                        <input type="text" id="searchName" name="searchName" class="form-control" placeholder=" بحث برقم الطلب">
                    </div>

                    <!-- تاريخ من -->
                    <div class="col-sm-12 col-md-4 col-lg-3 col-xl-2 mt-2">
                        <label for="startDate" class="mb-2">تاريخ من</label>
                        <input type="date" id="startDate" name="startDate" class="form-control">
                    </div>

                    <!-- تاريخ إلى -->
                    <div class="col-sm-12 col-md-4 col-lg-3 col-xl-2 mt-2">
                        <label for="endDate" class="mb-2">تاريخ إلى</label>
                        <input type="date" id="endDate" name="endDate" class="form-control">
                    </div>

                    <!-- زر البحث -->
                    <div class="col-sm-12 col-md-4 col-lg-3 col-xl-2 mt-2">
                        <button type="submit" style="color: white;" class="btn PrimaryBtn mt-4 w-100">بحث</button>
                    </div>
                </div>
            </form>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">رقم المستند</th>
                        <th scope="col">تاريخ المستند</th>
                        <th scope="col">تعديل</th>
                        <th scope="col">حذف</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var file in Model)
                    {
                        <tr>
                            <td>@counter</td>
                            <td>@file.DocumentNum</td>
                            <td>@(file.Documentdate?.ToString("MM/dd/yyyy"))</td>
                            <td>
                                <a href="@Url.Action("EditFile", "InFiles", new { id = file.Id })" class="btn text-dark">
                                    <img src="/images/edit.png" alt="تعديل" style="width: 30px; height: 30px;">
                                </a>
                            </td>
                            <td>
                                <form method="post" action="@Url.Action("DeleteFile", "InFiles")" class="d-inline">
                                    <input type="hidden" name="id" value="@file.Id" />
                                    <button type="button" class="btn text-dark delete-material" style="border:none; background:transparent;">
                                        <img src="/images/trash.png" alt="حذف" style="width:30px;height:30px;">
                                    </button>
                                </form>
                            </td>

                        </tr>
                        counter++;
                    }
                </tbody>
            </table>
        
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
      document.addEventListener('click', function (e) {
        if (e.target.closest('.delete-material')) {
            const btn = e.target.closest('.delete-material');
            const row = btn.closest('tr');
            const fileId = row.querySelector('input[name="id"]').value;
            // نافذة التأكيد
            Swal.fire({
                title: 'هل أنت متأكد؟',
                text: 'سيتم حذف هذا المستند نهائيًا!',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'نعم، احذف',
                cancelButtonText: 'إلغاء',
                reverseButtons: true,
                confirmButtonColor: '#d33'
            }).then((result) => {
                if (result.isConfirmed) {
                    // إرسال طلب حذف باستخدام Ajax
                    fetch(`/InFiles/DeleteFile?id=${fileId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/x-www-form-urlencoded',
                        },
                    })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            // حذف الصف من الواجهة
                            row.remove();
                            Swal.fire({
                                title: 'تم الحذف!',
                                text: 'تم حذف المستند بنجاح.',
                                icon: 'success',
                                timer: 1500,
                                showConfirmButton: false
                            });
                        } else {
                            Swal.fire('حدث خطأ', 'لم يتم الحذف من قاعدة البيانات.', 'error');
                        }
                    })
                    .catch(error => {
                        Swal.fire('حدث خطأ', 'لم يتم الحذف من قاعدة البيانات.', 'error');
                    });
                }
            });
        }
    });

   // مثال لدالة إعادة الترقيم إن كنت تستعملها
	function renumberRows() {
	  document.querySelectorAll('#subjectsBody .row-index')
			  .forEach((cell, idx) => cell.textContent = idx + 1);
	}

 
</script>

